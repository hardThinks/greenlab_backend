tags:
  - "quiz"
parameters:
  - in: "body"
    name: "body"
    required: true
    schema:
      $ref: "#/definitions/FinishQuizRequest"

responses:
  200:
    schema:
      $ref: "#/definitions/QuizResult"
